{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Postpone cards review",
  "properties": {
    "interval coefficient": {
      "type": "number",
      "default": 0.33,
      "description": "Ratio of the exta day to add to the interval when days are added. (0 for none, 1 too have a change in interval equal to the change in due date)"
    },
    "coefficient for negative": {
      "type": "number",
      "default": 0.33,
      "description": "Ratio of the exta day to add to the interval when days are removed. (0 for none, 1 too have a change in interval equal to the change in due date)"
    }
  }
}
